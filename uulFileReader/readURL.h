#pragma once
#include <string>
#include "curl\curl.h"
#include <fstream>
#include <algorithm>
#include <vector>
#include <cmath>

class readURL {
 private:

 public:
	 void standardReadFromString(char* URL);
};
